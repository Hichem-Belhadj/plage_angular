import { LettreCapitalePipe } from './lettre-capitale.pipe';

describe('LettreCapitalePipe', () => {
  it('create an instance', () => {
    const pipe = new LettreCapitalePipe();
    expect(pipe).toBeTruthy();
  });
});
