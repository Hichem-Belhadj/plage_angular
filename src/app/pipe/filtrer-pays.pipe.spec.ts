import { FiltrerPaysPipe } from './filtrer-pays.pipe';

describe('FiltrerPaysPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerPaysPipe();
    expect(pipe).toBeTruthy();
  });
});
